-- Entry point for the mod's runtime (control) stage. All logic lives under scripts/,
-- split by responsibility: state, filters, requester tracking, GUI, and reconciliation.
require("scripts.events")
